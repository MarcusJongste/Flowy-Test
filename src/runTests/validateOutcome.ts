import { type testResult, type unitTest } from "../types";

function validateOutcome(unitTest:unitTest,outcome:any,result:'error'|'result'):'success' | 'failure'{

}

export {
     validateOutcome as default
}